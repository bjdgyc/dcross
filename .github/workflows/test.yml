on:
  workflow_dispatch:
    inputs:
      chosen-os:
        required: true
        type: choice
        description: Which OS to run the tests on
        options:
          - Ubuntu
          - macOS
          - windows

jobs:
  test:
    runs-on: [ self-hosted, "${{ inputs.chosen-os }}" ]
    steps:
      - run: |
          echo Hello world!
          uname -a